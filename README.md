# alert_triage_mini_view
A small Next.js + TypeScript page that loads a provided JSON file of ~200 mock security alerts (fields: id, title, severity, status, source, createdAt, assignee) and lets an analyst triage them.
